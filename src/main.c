#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    /* This is a comment. */
    int distance = 100;

    // this is also a comment
    printf("You are %d miles away.\n", distance);

    return EXIT_SUCCESS;
}
